# =========================================================================
# Jornada Supera - Painel Administrativo
# Copie para `.env.local`. NUNCA comite um .env com valores reais.
# =========================================================================

# Fonte de dados: "mock" | "supabase"
# Fase atual = mock. Vira "supabase" apenas na Fase 15.
VITE_API_MODE=mock

# --- Simulacao do backend (so tem efeito com VITE_API_MODE=mock) ---
# Latencia artificial em ms (min-max) para os estados de loading serem reais
VITE_MOCK_DELAY_MIN=300
VITE_MOCK_DELAY_MAX=800
# Percentual de requisicoes que falham de proposito, para testar ErrorState (0 a 100)
VITE_MOCK_ERROR_RATE=0

# --- Supabase (Fase 15) ---
# A anon key e publica por design; a protecao real e a RLS no Postgres.
# A service_role key NUNCA entra no front-end.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Segundo fator ---
# Exigencia contratual no login do administrador. Ausente = true.
# So use "false" em desenvolvimento, e enquanto nao existir a tela de cadastro
# do aplicativo autenticador.
VITE_MFA_OBRIGATORIO=true

# --- Sessao ---
# Minutos de inatividade ate o logout automatico.
# 0 desliga a expiracao por completo — use apenas em desenvolvimento.
VITE_SESSION_IDLE_MINUTES=15
# Minutos antes do fim para exibir o aviso de expiracao
VITE_SESSION_WARN_MINUTES=2
